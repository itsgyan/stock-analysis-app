# ======================================================
# Stock Analysis - Spring Boot Application Configuration
# ======================================================

# --- Server ---
server.port=${PORT:8080}

# --- Application Name ---
spring.application.name=stock-analysis

# --- PostgreSQL Database ---
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/stockanalysis}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=${DB_USERNAME:postgres}
spring.datasource.password=${DB_PASSWORD:root}

# --- JPA / Hibernate ---
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=${JPA_SHOW_SQL:false}
spring.jpa.properties.hibernate.format_sql=${HIBERNATE_FORMAT_SQL:false}

# --- H2 Console (Disabled) ---
spring.h2.console.enabled=false

# --- Logging ---
logging.level.com.project.stockanalysis=${APP_LOG_LEVEL:INFO}
logging.level.org.springframework.web=INFO

# --- JWT Configuration ---
# Base64-encoded 256-bit secret key (change this in production!)
app.jwt.secret=${JWT_SECRET:dGhpcy1pcy1hLXZlcnktc2VjcmV0LWtleS1mb3Itc3RvY2stYW5hbHlzaXMtYXBwLTIwMjQ=}
# Token expiry: 24 hours in milliseconds
app.jwt.expiration-ms=86400000

# -------------------------------------------------------
# Alpha Vantage API Configuration
# Get your FREE key at: https://www.alphavantage.co/support/#api-key
# -------------------------------------------------------
alphavantage.api.key=${ALPHA_VANTAGE_API_KEY:DYV3MH28386UMPS6}
alphavantage.api.base-url=https://www.alphavantage.co/query
alphavantage.live-enabled=${ALPHA_VANTAGE_LIVE_ENABLED:false}
# Free tier: 25 requests/day, 5/min. Set to 'demo' for testing (limited symbols)
# Supported demo symbols: IBM, MSFT, AAPL (for some endpoints)

# --- HTTP Client Config ---
spring.mvc.async.request-timeout=30000
alphavantage.connect-timeout-ms=${ALPHA_VANTAGE_CONNECT_TIMEOUT_MS:1500}
alphavantage.read-timeout-ms=${ALPHA_VANTAGE_READ_TIMEOUT_MS:3000}

# --- WebSocket ---
spring.websocket.max-session-idle-timeout=600000

# --- CORS (allow frontend during development) ---
app.cors.allowed-origins=http://localhost:8080,http://localhost:3000
